---
"@azatakmyradov/opencode-git-plugin": patch
---

Prevent stalled Git generation dialogs by routing model requests through the server plugin, enforcing a timeout, propagating cancellation, and showing generation failures in persistent dialogs.
