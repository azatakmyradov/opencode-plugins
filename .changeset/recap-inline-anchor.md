---
"@azatakmyradov/opencode-recap-plugin": patch
---

Fix inline recap cards in the OpenCode 2 TUI when the saved anchor message has no matching rendered row. Placement now supports part-scoped row IDs and falls back to the transcript scroll container when no anchor is found.
